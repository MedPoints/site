

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">{{localize 'breadcrumbs.home'}}</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{PAGE_TITLE}}</li>
        </ol>
    </nav>
	<div class="row">
	
        <div class="col-xl-3 push-xl-9 col-lg-12 push-lg-0">

            <h3>{{localize 'drug.categories'}}</h3>

            <ul class="cat-list">
                <li><a class="active" href="/drugs">{{localize 'drug.categoriesAll'}}</a></li>
                {{#each categories as |category|}}
                    <li><a href="/drugs?group={{category.id}}">{{category.name}}</a><span class="badge">{{category.drugs}}</span></li>
                {{/each}}
            </ul><!--/.cat-list-->

        </div>
        <!--/.col-->
		
        <div class="col-xl-9 pull-xl-3 col-lg-12 pull-lg-0">
            <div class="table-responsive">
                <table summary="" class="table table-hover">
                    <thead>
                        <tr>
                            <th width="40%">
                                <h5>{{localize 'drugs.table.name'}}</h5>
                            </th>
                            <th>
                                <h5>{{localize 'drugs.table.price'}}</h5>
                            </th>
                            <th>
                                <h5>{{localize 'drugs.table.insurance'}}</h5>
                            </th>
                            <th>
                                <h5>{{localize 'drugs.table.prescription'}}</h5>
                            </th>
                            <th class="text-right">
                                <h5>{{localize 'drugs.table.providers'}}</h5>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each drugs as |drug|}}
                        <tr>
                            <td>
                                <h4><a href="/drugs/{{ drug.id }}" title="">{{ drug.name }}</a></h4>
                                <p>{{ drug.short_descr }}</p>
                            </td>
                            <td class="price-td">{{localize 'common.fromPrice'}} <strong class="text-dark">${{ drug.price.dollars }}</strong><br /><strong class="text-success">{{ drug.price.mpts }} MPT</strong></td>
                            <td class="text-center">
                                {{#if drug.is_covered_by_insurance}}
                                <i class="fas fa-plus text-success" data-toggle="tooltip" data-placement="top" title="Can be purchased for insurance points"></i>
                                {{/if}}
                            </td>
                            <td class="text-center">
                                {{#if drug.is_by_prescription}}
                                <i class="fas fa-plus text-success" data-toggle="tooltip" data-placement="top" title="Can be purchased for insurance points"></i>
                                {{/if}}
                            </td>
                            <td><a class="btn btn-primary float-right" href="/pharmacies?drugId={{drug.id}}"><strong>{{ drug.providers.pharmacies }}</strong> {{localize 'ratings.items.pharmacies'}}</a></td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <!--end of .table-responsive-->
			
			<br />
 
			<div class="row pagination-line">
            	<div class="col">
					<nav aria-label="...">
                    {{> pager pagerInfo }}
                </nav>
            	</div>
          	</div>

        </div>
        <!--/.col-->
    </div>
    <!--/.row-->
 