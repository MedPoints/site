<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">{{localize req 'breadcrumbs.home'}}</a></li>
        <li class="breadcrumb-item active" aria-current="page">{{localize req 'breadcrumbs.account'}}</li>
    </ol>
</nav>

<h3>{{localize req 'account.login'}}</h3>
<div class="button-container">
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#login">{{localize req 'account.loginButton'}}</button>
    <a class="btn btn-link" href="/registration">{{localize req 'account.registerButton'}}</a>
</div>
